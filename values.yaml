replicaCount: 1

frontend:
  image: dimeblyat/frontend
  tag: "latest"
  servicePort: 80

backend:
  image: dimeblyat/backend
  tag: latest
  servicePort: 5000

ingress:
  host: myapp.local

postgresql:
  enabled: false
  image:
    registry: docker.io
    repository: postgres
    tag: "16"
  global:
    security:
      allowInsecureImages: true
  primary:
    persistence:
      enabled: false
    podSecurityContext:
      enabled: false
    containerSecurityContext:
      enabled: false
